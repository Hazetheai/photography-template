{% if showHomeLink == true %}
  {% set headerClass = 'header--bg' %}
{% endif %}

{% if section != 'gallery-item' %}
  {% set headerBg = 'bg' %}
{% else %}
  {% set headerBg = '' %}
{% endif %}

<header class="header {{ headerClass }} {{ headerBg }} padding-lg z-index-header position-fixed " data-header="data-header">
  {# {% if showHomeLink == true %}
    <h3 class="header__title">
      <a href="/">{{ metadata.title }}</a>
    </h3>
  {% endif %} #}
  {# <button class="header__menu-btn" hidden="true" data-btn="menu" aria-haspopup="true">Menu</button> #}
  <div data-menu-wrapper="data-menu-wrapper">
    <nav class="header__nav">
      <ul class="header__list flex justify-between">
        {# <li class="header__item">
          <a class="header__link" href="/" hidden="true" data-home-link="data-home-link">Home</a>
        </li> #}
        <li class="header__item">
          <a class="header__link link-subtle" href="/about" >About</a>
        </li>
        <li class="header__item">
          <a class="header__link link-subtle" href="/" data-home-link="data-home-link">Rowan</a>
        </li>
        <li class="header__item">
          <a class="header__link link-subtle" href="/contact" >Contact</a>
        </li>

        {# {% for page in collections.page %}
          <li class="header__item">
            <a class="header__link" href="{{ page.url }}">{{ page.data.title }}</a>
          </li>
        {% endfor %} #}

        {# {% for item in menudata %}
          <li class="header__item">
            <a class="header__link" href="{{ item.url }}">
              {{ item.title }}
              <svg xmlns="http://www.w3.org/2000/svg" width="30" height="30">
                <use href="#{{ item.icon }}"/>
              </svg>
            </a>
          </li>
        {% endfor %} #}

      </ul>
    </nav>
  </div>
</header>